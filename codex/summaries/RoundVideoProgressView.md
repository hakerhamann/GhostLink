# RoundVideoProgressView.kt Summary

`RoundVideoProgressView` draws the circular progress ring used by round video recording and playback UI.

## Responsibilities

- Draw a subtle circular track.
- Draw current progress as a neon arc from the top.
- Expose a clamped `setProgressFraction` API for binders/controllers.
